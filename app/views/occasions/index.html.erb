<h1>Listing occasions</h1>

<table>
  <thead>
    <tr>
      <th>Message</th>
      <th>Name</th>
      <th>Date</th>
      <th>Occastion type</th>
      <th>Recipient</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @occasions.each do |occasion| %>
      <tr>
        <td><%= occasion.message %></td>
        <td><%= occasion.name %></td>
        <td><%= occasion.date %></td>
        <td><%= occasion.occastion_type %></td>
        <td><%= occasion.recipient %></td>
        <td><%= link_to 'Show', occasion %></td>
        <td><%= link_to 'Edit', edit_occasion_path(occasion) %></td>
        <td><%= link_to 'Destroy', occasion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Occasion', new_occasion_path %>
